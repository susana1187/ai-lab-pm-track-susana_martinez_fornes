First, a quick safety check: if this folder has no `src/` screen, no
`product_context/`, and no `PLAN.md`, then there is no Session 2 prototype here to
work from. Do NOT invent one. Stop and tell me exactly this: "I can't find a
prototype in this folder. Did you build one in Session 2? If not, open
~/ai-enablement-labs-pm/session-3-engineering-readiness/reference/what-good-looks-like.md and
follow along with the worked example instead."
Otherwise, continue.

Help me put a rough cost on building this for real, so the build decision is honest
about effort, not just about how good the idea sounds. You are estimating for a
conversation with engineering, not committing anyone to anything.

Read `PLAN.md`, the brief in `product_context/`, and the screen in `src/`. Write a
COST-OF-DEVELOPMENT WORKSHEET and save it as `cost-worksheet.md`. Use this shape:

- **Build effort**: a t-shirt size (S / M / L) for turning this prototype into a
  real feature, with one line of why.
- **What's actually involved**: the main pieces an engineer would have to build
  that the prototype only fakes (the real data behind the screen, the actions
  doing real work, permissions, error handling).
- **Dependencies**: other teams, systems, or data this would need.
- **Unknowns and risks**: the things that could make it much bigger than it looks.
- **Ongoing cost**: what it costs to run and maintain after launch, including any
  AI or infrastructure running behind it.
- **Value vs cost**: one honest line weighing the effort against the payoff the
  validation plan is chasing.

This is a PM's first cut, not an engineering estimate. Do not invent precise
numbers; where you are unsure, write it as a question for engineering. When it's
written, stop and ask me to review it.
