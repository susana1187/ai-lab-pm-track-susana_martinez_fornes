Based on what we've discussed and the brief in `product_context/`, write a short
plan for building this ONE screen. Do NOT build anything yet.

Write the plan in plain English a non-technical person can review, and save it as
`PLAN.md` in this folder. Use this shape:

- **What we're building**: one or two sentences.
- **What's on the screen**: a simple list of the parts (e.g. "a title", "a list
  of requests", "a filter", "an approve/deny button on each row").
- **Steps**: a short numbered list of the order you'll build it in, each step a
  plain phrase. Mark each `[ ] to do`.

Keep it minimal. Do not add features, screens, or complexity beyond the brief.
This is the moment for me to catch a misunderstanding before any code exists, so
make the plan easy to read and correct.

When the plan is written, stop and ask me to review it.
