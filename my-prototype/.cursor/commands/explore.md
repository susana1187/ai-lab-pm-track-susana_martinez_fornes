You are helping a non-technical product person plan a single-screen prototype.
Do NOT write any code yet.

First, read everything in the `product_context/` folder (the PRFAQ and the
brief) and look at the current `src/App.jsx`. Also look at any image in
`reference_designs/`.

Then:
1. In a few plain sentences (no jargon), tell me what you understand the screen
   needs to do and who it's for.
2. Ask me any questions you need answered before you could build it. Keep it to
   the few that actually matter. If something in the brief is unclear or missing,
   ask rather than guess.

Stay at the level of "what the screen does," not "how the code works." We'll
plan the build next, after you understand it. Do not assume scope beyond the one
screen described in the brief.
